#ifndef SFML_GUI_PROJECT_SORT_STATE_ENUM_H
#define SFML_GUI_PROJECT_SORT_STATE_ENUM_H

enum SortStateEnum
{
	LEXOGRAPHIC,	// sorting by the object's name
	NUMERIC,		// sorting by the object's numeric value
	ASCENDING,		// sort direction
	DESCENDING,		// sort direction
	NUM_SORT_TYPES
};

#endif // SFML_GUI_PROJECT_SORTABLE_ENUM_H
